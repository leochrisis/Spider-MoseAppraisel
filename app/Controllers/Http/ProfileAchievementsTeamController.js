'use strict'

/**
 * Resourceful controller for interacting with profileachievementsteams
 */
class ProfileAchievementsTeamController {
  /**
   * Show a list of all profileachievementsteams.
   * GET profileachievementsteams
   */
  async index ({ request, response, view }) {
  }

  /**
   * Render a form to be used for creating a new profileachievementsteam.
   * GET profileachievementsteams/create
   */
  async create ({ request, response, view }) {
  }

  /**
   * Create/save a new profileachievementsteam.
   * POST profileachievementsteams
   */
  async store ({ request, response }) {
  }

  /**
   * Display a single profileachievementsteam.
   * GET profileachievementsteams/:id
   */
  async show ({ params, request, response, view }) {
  }

  /**
   * Render a form to update an existing profileachievementsteam.
   * GET profileachievementsteams/:id/edit
   */
  async edit ({ params, request, response, view }) {
  }

  /**
   * Update profileachievementsteam details.
   * PUT or PATCH profileachievementsteams/:id
   */
  async update ({ params, request, response }) {
  }

  /**
   * Delete a profileachievementsteam with id.
   * DELETE profileachievementsteams/:id
   */
  async destroy ({ params, request, response }) {
  }
}

module.exports = ProfileAchievementsTeamController
